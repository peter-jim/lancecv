# 兰斯 · 个人网站

Lance's personal website — a self-contained single-page site.

## Deploy

This is a static site with a single `index.html` file. Deploy directly to Vercel:

```bash
npx vercel
```

No build step required — Vercel will serve `index.html` as-is.
